{
    "db":{
        "driver":"postgres",
        "name":"${DB_NAME}",
        "password":"${DB_PASSWORD}",
        "port":"${DB_PORT}",
        "host":"${DB_HOST}",
        "user":"${DB_USER}",
        "ssl_mode":"${DB_SSL_MODE}"
    }
}